from django.urls import path
from . import views

urlpatterns = [
    # Add any additional URL patterns for the home app here
    # For example:
    # path('some-path/', views.some_view, name='some_view'),
] 